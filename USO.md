## Para probar el programa:
* 1 en un terminal en la raiz del proyecto ejecutar el
   siguiente comando para encender el servidor:

   java -jar target/servidor.jar
* 2 en otra terminal en la raiz del proyecto ejecutar el
  siguiente comando para encender cliente:

  java -jar target/stress-client.jar 